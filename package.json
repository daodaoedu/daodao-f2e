{
  "name": "daodao-f2e",
  "version": "0.0.0",
  "private": true,
  "description": "DaoDao Frontend Monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "dev:website": "pnpm --filter @daodao/website dev",
    "dev:product": "pnpm --filter @daodao/product dev",
    "dev:mobile": "pnpm --filter @daodao/mobile dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "lint:fix": "biome lint --write .",
    "typecheck": "turbo run typecheck",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "docker:dev": "docker-compose -f docker/development/docker-compose.yml up --build",
    "docker:dev:down": "docker-compose -f docker/development/docker-compose.yml down",
    "docker:prod": "pnpm build && docker-compose -f docker/production/docker-compose.yml up -d --build",
    "docker:prod:down": "docker-compose -f docker/production/docker-compose.yml down",
    "docker:build": "pnpm build && docker-compose -f docker/production/docker-compose.yml up -d --build",
    "clean": "turbo run clean && pnpm dlx rimraf node_modules"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "husky": "^9.1.7",
    "rimraf": "^6.0.1",
    "turbo": "^2.7.2",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=20.19.4",
    "pnpm": ">=10.20.0"
  },
  "packageManager": "pnpm@10.20.0"
}
