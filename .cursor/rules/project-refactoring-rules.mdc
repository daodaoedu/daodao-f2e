---
description: 
globs: 
alwaysApply: true
---
# 專案重構規則 (Project Refactoring Rules)

## 重構目標架構 (Target Architecture)

本專案正在進行重構，目標是建立清晰的資料夾結構和依賴關係。以下是重構完成後的預期架構：

```
- components/
    - ui        # 所有共用元件
- constants     # 全域常數
- contexts      # React Context (共用狀態管理)
- features      # 功能模組 (包含商業邏輯相關的 components, hooks, contexts 與 utils)
- hooks         # 共用的自訂 hooks
- layout        # 版面配置元件
- pages         # 頁面元件 (僅負責組裝，不包含過多邏輯)
- public        # 靜態資源
- services      # 外部服務串接相關
    - _shared   # 提供給 services 共用的 schema
    - *         # 外部服務定義
- utils         # 工具函數
```

## 核心依賴原則 (Dependency Principles)

### 1. Services 層級規則
- **modules 不互相依賴**: [services](mdc:services) 下的各模組應保持獨立
- 專注於後端 API 串接、資料結構定義和驗證規則
- 參考現有結構: [services/resources](mdc:services/resources)

### 2. Features 層級規則
- **features 之間不互相依賴**: [features](mdc:features) 下的各功能模組應保持獨立，避免導致循環依賴
- 包含與商業邏輯相關的元件和 hooks
- 每個 feature 應該是自包含的功能單元
- 現有範例: [features/projects](mdc:features/projects), [features/resources](mdc:features/resources)

### 3. Components 層級規則
- **共用性原則**: [components](mdc:components) 下的元件應該是可重用的
- ui: 所有共用的 UI 元件
- 避免在共用元件中包含特定的商業邏輯

### 4. Pages 層級規則
- **組裝職責**: [pages](mdc:pages) 僅負責組裝各種元件
- 不應包含過多的商業邏輯
- 商業邏輯應委託給 features 或 hooks
- 保持 pages 的簡潔性和可讀性

### 5. Hooks 和 Contexts 規則
- **共用性**: [hooks](mdc:hooks) 和 [contexts](mdc:contexts) 應該是跨功能使用的
- 功能特定的 hooks 應放在對應的 features 中
- 現有範例: [hooks/useControlledState](mdc:hooks/useControlledState), [contexts/Auth](mdc:contexts/Auth)

## 重構指導原則

### 檔案移動規則
1. **商業邏輯元件** → 移動到對應的 `features/` 目錄
2. **可重用元件** → 移動到 `components/ui/`
3. **API 相關檔案** → 移動到 `services/`
4. **共用 hooks** → 保留在 `hooks/`，功能特定的移到 `features/`
5. **共用 contexts** → 保留在 `contexts/`，功能特定的移到 `features/`
6. **共用 utils** → 保留在 `utils/`，功能特定的移到 `features/`

### import 規則
```typescript
// ✅ 正確的 import 方向
import { Button } from '@/components/ui/button'
import { useAuth } from '@/contexts/Auth'
import { resourceAPI } from '@/services/resources'
import { useProjects } from '@/features/projects'

// ❌ 避免的 import 方向
// features 之間互相依賴
import { useResources } from '@/features/resources' // 在 features/projects 中

// services 之間互相依賴
import { uploadImages } from '@/services/image' // 在 services/outcome 中
```

### 程式碼品質要求
- 使用 TypeScript 進行類型定義，避免使用 `any`，必須明確定義型別
- 遵循 ESLint 規則: [.eslintrc.js](mdc:.eslintrc.js)
- 使用 shadcn/ui 進行元件實作
- 使用 Tailwind CSS 進行樣式設計: [tailwind.config.ts](mdc:tailwind.config.ts)
- 遵循 React 最佳實踐和 hooks 規則

### UI 元件庫遷移要求
- **禁止使用**: Material-UI (MUI)、emotion、dayjs、react-icons
- **必須使用**: shadcn/ui + Radix UI 元件、date-fns、Tailwind CSS、自定義 icon、Lucide React 圖示
- 詳細遷移規範請參考: [UI 元件庫遷移規則](mdc:.cursor/rules/ui-migration-rules.mdc)

### Services API 與 Schema 定義規則
- 每個服務模組應至少包含 core 資料夾，包含 API 介面、Schema 定義和通用邏輯
- 使用 Zod 進行資料驗證和類型轉換
- 參考實作: [services/resources](mdc:services/resources)

#### 基本結構
```
services/
  ├── _shared/           # 共用的 schema 定義
  ├── resources/         # 資源服務模組
  │   ├── core/          # 核心 API 和 schema
  │   │   ├── api.ts     # API 介面定義
  │   │   ├── schema.ts  # 資料結構定義
  │   │   └── index.ts   # 導出檔案
  │   ├── reviews/       # 子模組
  │   └── index.ts       # 模組導出
  └── [其他服務模組]/
```

#### API 定義規範
- 使用 TypeScript 介面定義 API 行為
- 明確定義返回類型與參數類型
- 使用 SWR 或 SWR mutation 進行資料獲取
- 參考以下實作 [services/resources/core/api.ts](mdc:services/resources/core/api.ts)：

```typescript
interface ResourceAPIType {
  read: (resourceId: string) => Promise<ResourceDetailResponseSchema>;
  readList: () => Promise<ResourceListResponseSchema>;
  create: MutationFetcher<
    ResourceMutationResponseSchema,
    string,
    ResourceFormSchema
  >;
  update: MutationFetcher<
    ResourceMutationResponseSchema,
    string,
    ResourceFormSchema
  >;
  delete: MutationFetcher<void, string, { id: number }>;
}

export const resourceAPI: ResourceAPIType = {
  read: (resourceId) => {
    const id = parseToNumber(resourceId);
    if (typeof id !== "number") {
      throw new Error("Invalid resource id");
    }
    return fetcher<ResourceDetailResponseSchema>(
      getResourcePathname({ id })
    ).catch(() => mockResourceList.resources[0]);
  },
  readList: () =>
    fetcher<ResourceListResponseSchema>(getResourcePathname()).catch(
      () => mockResourceList
    ),
  create: mutations.post,
  update: mutations.put,
  delete: mutations.delete,
};
```

#### Schema 定義規範
- 使用 Zod 定義資料驗證和轉換邏輯
- 匯出 TypeScript 類型定義
- 細分 Schema 為 request/response 形式
- 參考以下實作 [services/resources/core/schema.ts](mdc:services/resources/core/schema.ts)：

```typescript
// 自定義 URL 驗證
const httpsUrl = z.string().url().refine(
  (url) => url.startsWith("https://"),
  { message: "URL 必須以 https:// 開頭" }
);

// 核心資料模型定義
export const resourceSchema = z.object({
  id: z.number(),
  resourceName: z.string().min(1, "請輸入資源名稱"),
  resourceUrl: httpsUrl,
  resourceImgUrl: httpsUrl.optional().nullable(),
  description: z.string(),
  // 更多欄位...
});

// 請求和響應模型
export const resourceListResponseSchema = z.object({
  resources: z.array(resourceSchema),
  pagination: z.object({
    limit: z.number(),
    hasNext: z.boolean(),
    hasPrev: z.boolean(),
    nextCursor: z.string().nullable(),
    prevCursor: z.string().nullable(),
    totalEstimate: z.number(),
  }),
});

export const resourceFormSchema = z.object({
  resourceName: z.string().min(1, "請輸入資源名稱"),
  resourceUrl: httpsUrl,
  resourceImgUrl: httpsUrl.optional(),
  // 更多欄位...
});

// 類型導出
export type ResourceSchema = z.infer<typeof resourceSchema>;
export type ResourceListResponseSchema = z.infer<typeof resourceListResponseSchema>;
export type ResourceFormSchema = z.infer<typeof resourceFormSchema>;
```

子模組的 schema 可以依賴核心模組，例如 [services/resources/reviews/schema.ts](mdc:services/resources/reviews/schema.ts)：

```typescript
export const resourceReviewSchema = z.object({
  id: z.number(),
  title: z.string().min(1, "請輸入心得標題"),
  content: z.string().min(1, "請輸入心得內容"),
  overallImpact: z.number().min(1).max(5),
  changeMindset: z.number().min(1).max(5),
  // 更多欄位...
});

export const resourceReviewFormSchema = z.object({
  title: z.string().min(1, "請輸入心得標題"),
  content: z.string().min(1, "請輸入心得內容"),
  overallImpact: z.number().min(1).max(5),
  // 更多欄位...
});

// 類型導出
export type ResourceReviewSchema = z.infer<typeof resourceReviewSchema>;
export type ResourceReviewFormSchema = z.infer<typeof resourceReviewFormSchema>;
```
